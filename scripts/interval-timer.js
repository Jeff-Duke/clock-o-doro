class Interval {
  constructor(duration) {
    }
    //make a new Interval Timer
    //takes an input in minutes
    //converts those to seconds
    //calls a countdown function that uses timeout every 1000ms
    //make the remaining time human readable and put it on the page


}
